# Bitácora de desarrollo

Registro cronológico de avances técnicos del proyecto **NS PAC Integration Facturama**.

### 2026-08-01 — Resumen inicial de la integración NetSuite–Facturama

- **Estado:** pendiente de validación
- **Objetivo o problema:** Implementar un método de envío de E-Documents que conecte NetSuite con Facturama API-Lite para timbrar CFDI 4.0, recuperar el XML certificado y actualizar la transacción de origen.
- **Cambios realizados:** Se inicializó un proyecto SDF de tipo Account Customization y se incorporó un plug-in de envío en SuiteScript 2.0. El flujo implementado recibe el JSON generado por la plantilla de E-Document, obtiene la configuración del PAC por subsidiaria, autentica la petición, solicita el timbrado, descarga y guarda el XML, actualiza campos de certificación CFDI y genera/adjunta un PDF. El repositorio conserva una versión monolítica de fase inicial y una versión modular separada por configuración, API, manejo de archivos y actualización fiscal. También incluye registros personalizados para configuración y logging, plantillas FreeMarker, payloads de ejemplo y material técnico de referencia.
- **Evidencia:** Commit inicial [`999df9c`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/999df9cc28e2ba1a8fc545e6b7a4b221a34ce73c).

### 2026-08-07 — Timbrado de Cash Sale y ajustes de estado y auditoría

- **Estado:** pendiente de validación
- **Objetivo o problema:** Completar la fase inicial del Sending Method para timbrar ventas de contado mediante Facturama y lograr que NetSuite actualice correctamente los campos, el estado y el Historial de Auditoría del documento electrónico.
- **Evidencia:** Commit [`3322e51`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/3322e5138ab4793baa7d013b13c1280c07a02376) y merge [`cb65f02`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/cb65f026bf3fcb12f26ce6a14cf9fc4b45490337).

### 2026-08-14 — Retorno nativo, PDFs certificados y manejo de respuestas del PAC

- **Estado:** pendiente de validación
- **Objetivo o problema:** Integrar el Sending Method con el ciclo de vida nativo de E-Documents, permitir que NetSuite genere el Historial de Auditoría correctamente, producir el PDF certificado según el tipo de transacción y distinguir un timbrado exitoso de errores posteriores en la recuperación del XML.
- **Evidencia:** Commits [`9e8defb`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/9e8defb45bfa8d84bdc8ddd0f340d24c4ffd7be8), [`cd3501b`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/cd3501b8af552502ea929a2a1554b7a32d95dbe8) y [`535a06e`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/535a06eea5b60dd0285b376230c76f01f27f2be4).

### 2026-08-17 — Refactor defensivo de comunicación PAC y construcción de campos CFDI

- **Estado:** pendiente de validación
- **Objetivo o problema:** Mejorar la mantenibilidad y la tolerancia a fallos del Sending Method ante errores de red, respuestas no válidas del PAC y fallos parciales al construir los campos fiscales.
- **Cambios realizados:** Se reforzó el adaptador HTTP con parseo seguro, captura centralizada de errores y contexto de diagnóstico para el timbrado y la descarga del XML. El constructor CFDI centralizó los IDs de campos, sustituyó la extracción por expresión regular del certificado del emisor por análisis con `N/xml`, aisló los errores de fecha/XML y conserva los campos construidos antes de un fallo parcial. En el orquestador se normalizó el alcance y retorno de `objReturn`.
- **Archivos o componentes:** SuiteScript 2.0 `pi_sads_fama_connector.js`; `lib/sads_fama_api.js`; `lib/sads_fama_cfdi.js`.
- **Evidencia:** Commit provisional [`b7dd46d`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/b7dd46ded32170025937029be233d88f5baa1ee7), integrado en `main` mediante [`bb4d03c`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/bb4d03cc170ca3f92e106b384365e8266d1fe81d).
- **Resultado:** El código incorpora mejor trazabilidad de excepciones HTTP y permite recuperar parcialmente campos fiscales cuando falla el procesamiento de fecha o XML. El cambio permanece sin validación funcional en NetSuite.
- **Problemas y riesgos:** El orquestador continúa enviando el payload mediante `N/https.post` directamente, por lo que la nueva protección de `apiModule.postTimbrado` todavía no participa en el flujo real. `getXml` puede devolver `null` o un cuerpo sin `Content`; el orquestador accede a `xmlData.Content` sin validar el objeto y, cuando solo falta `Content`, puede intentar guardar `XML_VACIO` como documento certificado.
- **Pendientes:** Conectar el orquestador con el adaptador `postTimbrado`; validar explícitamente `xmlData` y `Content`; definir el resultado del documento cuando el timbrado fue exitoso pero falla una etapa local; ejecutar pruebas controladas.

### 2026-08-24 — Arquitectura asíncrona para facturación global y descarga unificada de XML/PDF

- **Estado:** pendiente de validación
- **Objetivo o problema:** Incorporar una herramienta de facturación global con Facturama para seleccionar ventas de contado, generar un CFDI global de forma asíncrona y conservar los archivos certificados proporcionados por el PAC.
- **Cambios realizados:** Se añadió un Suitelet con Client Script para capturar parámetros fiscales y seleccionar Cash Sales. El envío crea un registro de control y ejecuta un Map/Reduce que consulta las transacciones, construye el payload CFDI 4.0 mediante un mapper dedicado, timbra con Facturama y actualiza cada Cash Sale con UUID, XML y PDF. El adaptador HTTP sustituyó `getXml` por `getFile` para descargar distintos tipos de archivo, mientras que el gestor de archivos reemplazó `saveXml` por `saveFile` y diferencia XML y PDF según la extensión. El flujo descarga ahora ambos comprobantes directamente del PAC y envía una notificación con los archivos adjuntos. También se incorporó el identificador de respuesta de Facturama en `custbody_sads_fama_cfdi_resp_id` para el flujo individual.
- **Archivos o componentes:** SuiteScript 2.x `fama_global_invoice_suitelet.js`, `fama_global_invoice_client.js`, `fama_global_invoice_library.js`; Map/Reduce `sads_fama_mr_global_orchestrator.js`; mapper `lib/sads_fama_global_mapper.js`; adaptadores `lib/sads_fama_api.js` y `lib/sads_fama_files.js`; orquestador individual `pi_sads_fama_connector.js`; fuentes de referencia bajo `Tests/Pruebas de E Docs NS CLI API/Scripts/com.drt.globalInvoice/`.
- **Evidencia:** Commits [`e9ce636`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/e9ce6363a5fff13b5af58db8a252c36c047371bc), [`f97b837`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/f97b837a0b17591ada678a9abb3f70c2abc83eba), [`5eabfd5`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/5eabfd54c893b45d072c659993778922805ad235) y [`2e74e6a`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/2e74e6a45eff976a608f1ef975e914c751226851). La comparación desde la última actualización documental contiene seis commits y 21 archivos afectados. Los mensajes de los commits indican explícitamente que las pruebas continúan pendientes.
- **Resultado:** La inspección confirma una arquitectura Suitelet–Map/Reduce implementada y la integración de las interfaces genéricas `getFile` y `saveFile` para XML/PDF. No existe evidencia de ejecución end-to-end, despliegue SDF ni timbrado global exitoso en NetSuite, por lo que el avance no se considera completado.
- **Decisiones técnicas:** Se separó la interfaz de captura del procesamiento fiscal mediante un registro de control y un Map/Reduce. La construcción del payload se aisló en un mapper y se decidió utilizar el PDF certificado entregado por Facturama, evitando generarlo localmente para la factura global.
- **Problemas y riesgos:** Los errores individuales de `reduce` se capturan sin relanzarse; por ello `summarize` puede reportar éxito aunque alguna Cash Sale no haya sido actualizada. Si falla `map`, el registro de control puede quedar sin estado final porque `summarize` obtiene su ID únicamente desde las salidas exitosas. No se localizaron objetos SDF para los nuevos scripts, despliegues o parámetros, por lo que falta confirmar cómo se instalarán y configurarán. El adaptador HTTP registra respuestas completas del PAC, potencialmente incluyendo contenido Base64 o datos fiscales, y requiere revisar volumen, redacción y permisos de los logs.
- **Pendientes:** Desplegar y configurar el Suitelet, Client Script y Map/Reduce en sandbox; validar los IDs de scripts, despliegues, parámetros, custom record, campos y carpeta de destino; corregir la propagación y contabilización de errores parciales; ejecutar pruebas con una y múltiples Cash Sales; comprobar payload, timbrado, UUID, XML, PDF, correo y actualización del registro de control; medir gobernanza; revisar protección de credenciales y datos fiscales en logs.
- **Referencias:** Comparación de `main` entre [`9557633...2e74e6a`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/compare/9557633b59e3159d8d97d019450327e72e31be37...2e74e6a45eff976a608f1ef975e914c751226851).

### 2026-08-31 — Despliegue SDF y validación parcial del payload de facturación global

- **Estado:** pendiente de validación
- **Objetivo o problema:** Preparar la herramienta de facturación global para su instalación en producción y corregir la construcción de los conceptos del CFDI antes de habilitar el timbrado con Facturama.
- **Cambios realizados:** Se incorporaron objetos y dependencias SDF para el Suitelet y el Map/Reduce, incluidos despliegues, parámetros, campos de transacción, registro de configuración y workflow. El orquestador ajustó la consulta de Cash Sales para excluir líneas principales, impuestos, envío y COGS; añadió columnas explícitas para artículo, cantidad, importes, descuentos, objeto de impuesto y tasa; y normalizó los datos enviados al mapper. El mapper actualizó la estructura de `Items`, impuestos, descuentos, precio unitario y total. También se añadió el tratamiento de fecha de emisión y la extracción de códigos SAT para mes y forma de pago.
- **Archivos o componentes:** SuiteScript 2.0 Map/Reduce `sads_fama_mr_global_orchestrator.js`; mapper `lib/sads_fama_global_mapper.js`; Suitelet `fama_global_invoice_suitelet.js`; objetos SDF `customscript_fama_global_invoice_sl.xml`, `customscript_sads_fama_mr_global_orch.xml`, campos de transacción, registro de configuración, workflow y `manifest.xml`.
- **Evidencia:** Commits [`0e27146`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/0e2714693a58b362379b037218facd14c553cd07), merge [`c83e3da`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/c83e3dab57e265771824679b9835507a77642c1f) y correcciones [`094fd92`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/094fd92081d09a3baf809afbff8e235f229ffd36). La comparación desde la actualización documental anterior contiene tres commits y 19 archivos afectados. El autor reporta un despliegue exitoso y pruebas en producción sin certificación.
- **Resultado:** Los objetos necesarios quedaron incorporados al proyecto SDF y el payload pudo construirse con datos obtenidos de Cash Sales. La evidencia disponible valida únicamente la generación del JSON; no confirma timbrado, certificación, descarga de XML/PDF ni actualización de transacciones.
- **Decisiones técnicas:** El envío al PAC permanece deshabilitado mediante un bloque comentado y el Map/Reduce funciona temporalmente como simulador, registrando el payload sin transmitirlo a Facturama.
- **Problemas y riesgos:** Los valores SAT de `Months` y `PaymentMethod` continúan pendientes de corrección; `PaymentMethod` está fijado en `PUE` y la periodicidad en `01`. El despliegue del Map/Reduce usa el rol `ADMINISTRATOR` y nivel `DEBUG`, y registra el payload y las líneas extraídas, por lo que debe revisarse la exposición de datos fiscales. No hay evidencia independiente que confirme el despliegue o las pruebas indicadas en los mensajes de commit.
- **Pendientes:** Corregir y validar mes, periodicidad y método de pago contra los catálogos SAT; completar la carga del CSD en Facturama; revisar permisos y logging; habilitar nuevamente el bloque de timbrado; ejecutar una prueba controlada con certificación y comprobar UUID, XML, PDF, correo, actualización de Cash Sales, manejo de errores y consumo de gobernanza.
- **Referencias:** Comparación de `main` entre [`fd52460...094fd92`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/compare/fd5246033fc71e038a0a7efc6b92b7e05983f4f4...094fd92081d09a3baf809afbff8e235f229ffd36).

### 2026-09-08 — Cierre de facturación global e inicio del complemento de pagos

- **Estado:** en progreso
- **Objetivo o problema:** Completar el flujo de facturación global con Facturama, cerrar la plantilla para facturas de venta y comenzar la preparación del CFDI con complemento de pagos.
- **Cambios realizados:** Se terminó la adaptación del Map/Reduce de facturación global para timbrar, descargar XML/PDF, actualizar las Cash Sales y el registro de control, y enviar la notificación final. Se reforzaron la selección y serialización de transacciones, el tratamiento de fechas, la validación de la respuesta del PAC y el redondeo del mapper a seis decimales. La plantilla FreeMarker de factura de venta incorporó validaciones de método y forma de pago, CFDI relacionados, emisor, receptor, conceptos, traslados y retenciones. Para la siguiente etapa se añadieron payloads de referencia del complemento de pagos y comenzaron ajustes en el constructor CFDI y el mapper.
- **Archivos o componentes:** SuiteScript 2.0 Map/Reduce `sads_fama_mr_global_orchestrator.js`; módulos `lib/sads_fama_global_mapper.js`, `lib/sads_fama_cfdi.js`, `lib/sads_fama_config.js` y `lib/sads_fama_files.js`; plantilla FreeMarker `facturama_edocs_template.ftl`; ejemplos `EJ_peticion_cfdi_cpago_facturama.json` y `.txt`; objetos SDF de configuración y `manifest.xml`.
- **Evidencia:** El commit [`166b957`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/166b95799fcde0d224e0eb80ac513bd5f4494410) reporta una respuesta HTTP 201 en pruebas y deja pendientes campos del registro de control. El commit [`37076b0`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/37076b0d9acaa07b1d6046bc2772b88914c30a81) reporta creación exitosa de registros y timbres después de pruebas. El commit [`638d363`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/commit/638d363c83eff280026dda3c0819fcd6abae2f0a) declara terminada la plantilla de factura de venta e inicia el complemento de pagos. La comparación desde la actualización documental anterior contiene cuatro commits y 12 archivos afectados.
- **Resultado:** La facturación global queda reportada como funcional y probada, incluidos el timbrado y la creación de registros. La plantilla de factura de venta queda terminada en código; el complemento de pagos permanece en etapa inicial. No se aportaron capturas, logs de ejecución ni casos de prueba reproducibles para validación independiente.
- **Decisiones técnicas:** El procesamiento continúa distribuido mediante Map/Reduce; el PAC proporciona los archivos certificados y la configuración define carpetas separadas para XML/PDF. La plantilla detiene el timbrado cuando faltan datos SAT obligatorios o cuando una operación PPD no utiliza la forma de pago 99.
- **Problemas y riesgos:** Se confirmó que el objeto SDF `customrecord_sads_fama_config.xml` contiene credenciales en texto claro; sus valores se omiten deliberadamente de esta bitácora. Deben considerarse comprometidas, rotarse y retirarse del historial y de los objetos versionados. El manejo de errores de `reduce` todavía captura fallos sin relanzarlos, por lo que el resumen puede no contabilizar todas las Cash Sales fallidas. No existe evidencia de medición de gobernanza para el procesamiento masivo.
- **Pendientes:** Rotar inmediatamente las credenciales expuestas, limpiar el historial y migrar los secretos a una alternativa segura de NetSuite; ejecutar y conservar pruebas reproducibles de facturación global; confirmar estados y archivos en el registro de control; corregir la propagación de errores parciales; medir gobernanza; implementar y validar el módulo, plantilla y timbrado del complemento de pagos.
- **Referencias:** Comparación de `main` entre [`6e58d17...638d363`](https://github.com/saulkgames/NS_PAC_Integration_Facturama/compare/6e58d170a676ff24d345fbbffd8a63e8633d9e22...638d363c83eff280026dda3c0819fcd6abae2f0a).

### 2026-09-21 — Complemento de Pago (User Events), validación SDF y hardening de repositorio

- **Estado:** en progreso
- **Objetivo o problema:** Construir el arreglo `RelatedDocuments` del Complemento de Pago para facturas PPD sin recalcular en tiempo de timbrado, y resolver los errores bloqueantes reportados por `suitecloud project:validate` antes del siguiente deploy.
- **Cambios realizados:** Se agregó un User Event en Customer Payment (`fama_payment_complement_ue.js`) que, al guardar el pago, filtra las líneas aplicadas a facturas cuyo `custbody_mx_txn_sat_payment_term` resuelve a PPD, calcula `PartialityNumber` y `PreviousBalanceAmount`/`ImpSaldoInsoluto` a partir del historial de pagos previos ya certificados (`PreviousTransactionLink`, excluyendo el pago actual) y persiste el JSON resultante en `custbody_sads_fama_cpago_payload`. Un segundo User Event en Invoice (`fama_invoice_tax_object_ue.js`) cachea el `ObjetoImp` SAT en `custbody_sads_fama_tax_object` para evitar recalcularlo por cada pago. Ambos comparten el helper `lib/sads_fama_sat_catalog.js`. Se corrigió `manifest.xml` agregando como dependencias los 12 roles referenciados en `audslctrole` de `customscript_fama_global_invoice_sl.xml`, que bloqueaban la validación. Se depuraron los objetos XML nuevos (campos custom y deployments) de tags no soportados por el esquema de la cuenta (`aidescription`, `enabletextenhance`, `bodyvendorprepayment`, `title` en deployment de User Event).
- **Archivos o componentes:** `fama_payment_complement_ue.js`, `fama_invoice_tax_object_ue.js`, `lib/sads_fama_sat_catalog.js`; objetos SDF `custbody_sads_fama_cpago_payload.xml`, `custbody_sads_fama_tax_object.xml`, `customscript_sads_fama_payment_ue.xml`, `customscript_sads_fama_invoice_ue.xml`; `manifest.xml`; `.gitignore`.
- **Resultado:** `suitecloud project:validate` (local y contra la cuenta sandbox) ya no reporta errores ni warnings sobre los objetos nuevos, y el error bloqueante de `audslctrole` quedó resuelto. El Complemento de Pago no se ha probado con un timbrado real.
- **Decisiones técnicas:** `EquivalenceDocRel` se calcula como `1/exchangerate` de la factura solo cuando su moneda difiere de la del pago; queda marcado en el propio código como supuesto no validado contra un timbrado real. El script falla en silencio (registra el error vía `sads_fama_logger` sin relanzarlo) para no bloquear el guardado del pago ante un error de cálculo del Complemento.
- **Problemas y riesgos:** Se reverificó el riesgo de credenciales en texto claro reportado el 2026-08-24: `customrecord_sads_fama_config.xml` **continúa** conteniendo credenciales reales sin rotar, presentes en el working tree actual y en la totalidad del historial de `main` (desde el commit inicial `999df9c` hasta `0dc75ad`). Se reforzó `.gitignore` con reglas de higiene y una nota explícita para no volver a exportar datos de instancia de ese objeto, pero esto no elimina la exposición ya ocurrida: la rotación de credenciales y la limpieza del historial de git siguen sin ejecutarse. `PartialityNumber`/`PreviousBalanceAmount` asumen que ningún pago PPD queda sin timbrar (política interna); si esa política se incumple alguna vez, el conteo se desincroniza sin aviso.
- **Pendientes:** Rotar las credenciales de Facturama y reescribir el historial de git para retirarlas (acción destructiva que requiere coordinación explícita antes de ejecutarse); validar en sandbox la dirección de `EquivalenceDocRel` con un timbrado real; confirmar el valor de lista SAT de PPD (`"4"`) en esta cuenta; crear una búsqueda guardada que detecte pagos PPD sin `custbody_sads_fama_cpago_payload` poblado; resolver el `ACCOUNT_SPECIFIC_VALUE` de `custrecord_sads_fama_sub` en `customrecord_sads_fama_config.xml` (pendiente de que se indique la subsidiaria correcta).

### 2026-09-23 — Pruebas reales de Complemento de Pago en sandbox: bugs de datos, consolidación de capas y generación de E-Document aún fallando

- **Estado:** en progreso — bloqueado en la generación del E-Document
- **Objetivo o problema:** Cerrar el ciclo completo del Complemento de Pago con una prueba real en sandbox (factura PPD → Customer Payment → generación del documento electrónico), y corregir los defectos que aparecieran en el camino.
- **Cambios realizados:**
  - **Corrección de datos reales:** con un pago real aplicado a dos facturas PPD (IDs 2052103/2052104, pago 2052106), se detectaron y corrigieron dos bugs en `fama_payment_complement_ue.js`: (1) `Serie`/`Folio` llegaban `null` porque esas facturas nunca tuvieron pobladas `custbody_mx_cfdi_serie`/`folio` — se agregó `_decomposeTranId()` como respaldo, descomponiendo el `tranid` (ej. `"B1653380"` → Serie `"B"`, Folio `"1653380"`); (2) `Base`/`Total` de `Taxes` salían negativos e idénticos entre sí — se determinó que `transactionTaxDetail.basetaxamount` no es la base gravable (ese es `taxbasis`, ya removido en esta cuenta) sino el importe de impuesto en moneda base; se quitó esa columna y `Base` se deriva matemáticamente como `Tax/Rate`, con `Math.abs()` sobre `taxamount` (mismo patrón que `discountamount` en `sads_fama_global_mapper.js`).
  - **Validación estructural:** se agregó `_validateRelatedDocuments()` en `fama_payment_complement_ue.js`, que recorre el arreglo completo acumulando TODOS los errores encontrados (campos vacíos, UUID mal formado, valores no numéricos/`NaN`, `Taxes` vacío con `TaxObject` gravable, etc.) y los reporta en una sola escritura a `sads_fama_logger` antes de abortar el guardado del campo (Fail-Fast; no bloquea el guardado del pago).
  - **Consolidación de capas:** se detectó que la plantilla FreeMarker (`facturama_customerpayment_template.ftl`) reconstruía `RelatedDocuments` desde `custom.appliedTxns` (el hook nativo del bundle Mexico Compliance) con lógica propia (filtro implícito, `TaxObject` fijo en `"02"`), duplicando lo que `fama_payment_complement_ue.js` ya calculaba correctamente hacia `custbody_sads_fama_cpago_payload`. Se corrigió la plantilla para que sea un adaptador puro (solo inserta el campo ya armado) y se eliminó el bloque de enriquecimiento redundante en `pi_sads_fama_connector.js` (`_enrichRelatedDocuments`, `_fetchRelatedInvoiceData`, ~130 líneas), que hacía el mismo cálculo de `TaxObject`/`EquivalenceDocRel` por una segunda vía.
  - **Depuración de la generación del E-Document:** primer intento de generación falló con mensaje genérico ("se produjo un error durante la generación") sin detalle en el mensaje de UI; se agregaron guardas `<#stop>` explícitas en la plantilla para los campos de cabecera sin verificar (`satCodes.paymentMethod`, RFC/nombre/régimen fiscal del receptor, código postal). El Historial de Auditoría del documento electrónico reveló el error real: *"la generación falló, porque el documento electrónico resultante no es un archivo XML o JSON con formato correcto"* — se identificó que `${transaction.custbody_sads_fama_cpago_payload}` no llevaba ningún tratamiento de escape (a diferencia de todos los demás valores de la plantilla, que usan `?json_string`), por lo que el motor de FreeMarker probablemente auto-escapaba las comillas del JSON embebido; se agregó `?no_esc`. Tras reintentar, el usuario reporta un error distinto, todavía sin diagnosticar.
- **Archivos o componentes:** `fama_payment_complement_ue.js`, `pi_sads_fama_connector.js`, `facturama_customerpayment_template.ftl`. Se creó también `CLAUDE.md` en la raíz del repo con las convenciones de arquitectura del proyecto (para que viajen con git independientemente de la ruta absoluta de cada máquina de trabajo).
- **Resultado:** El payload en `custbody_sads_fama_cpago_payload` ya se genera correcto (Serie/Folio, Base/Tax positivos, validado estructuralmente). La generación del E-Document para Customer Payment sigue sin completarse — el fix de `?no_esc` no se ha confirmado como suficiente; hay un error posterior reportado por el usuario, pendiente de compartir el detalle y diagnosticar.
- **Decisiones técnicas:** Se estableció explícitamente (y se documentó en `CLAUDE.md`) que las plantillas FreeMarker no deben contener lógica de negocio bajo ninguna circunstancia — son adaptadores puros; toda decisión (filtros, cálculos, valores por defecto) vive en los User Events/módulos `lib/*.js`.
- **Problemas y riesgos:** La causa raíz del error de generación actual (post-`?no_esc`) no está confirmada — se está avanzando por hipótesis sucesivas contra un sandbox real, sin acceso directo del asistente a la cuenta de NetSuite para inspeccionar el Historial de Auditoría o el contenido crudo generado. Persisten sin resolver: rotación de credenciales, `EquivalenceDocRel` sin validar con timbrado real, ausencia de un mecanismo de backfill ejecutado para facturas PPD antiguas.
- **Pendientes:** Diagnosticar el nuevo error de generación reportado tras el fix de `?no_esc`; una vez que el E-Document genere correctamente, probar el timbrado real contra Facturama; correr `fama_backfill_tax_object_mr.js` en sandbox; los pendientes ya listados en la entrada del 2026-09-21 siguen abiertos.

### 2026-09-24 — `?no_esc` fue una regresión: revertido; pendiente inspeccionar el texto crudo generado

- **Estado:** en progreso — bloqueado en la generación del E-Document
- **Objetivo o problema:** Diagnosticar el error posterior al fix de `?no_esc` del 2026-09-23.
- **Cambios realizados:** El usuario compartió el detalle real del Historial de Auditoría (no visible en el mensaje genérico de UI): `UNEXPECTED_ERROR` con `message: null`, con stack trace mostrando que la excepción ocurre dentro de `N/render.renderAsString` (bundle 436209, `ns_wrapper_template_renderer.js` → `app_einvoice_generator.js:363` → `app_einvoice_manager.js:436`). A diferencia del error anterior (render exitoso, pero resultado no es JSON válido), este es un fallo *durante* el render mismo. Dado que apareció justo después de agregar `?no_esc`, se revirtió ese cambio en `facturama_customerpayment_template.ftl` — hipótesis: `?no_esc` no es un built-in válido en el contexto restringido en que el bundle de Electronic Invoicing invoca `N/render` (distinto del contexto de una plantilla PDF/HTML normal, donde si funcionaría).
- **Archivos o componentes:** `facturama_customerpayment_template.ftl`.
- **Resultado:** Sin confirmar todavía — el revert regresa al estado del 2026-09-23 (renderiza, pero el JSON resultante es inválido), aún sin probar en sandbox.
- **Decisiones técnicas:** Se decidió NO intentar una tercera variante de escapado a ciegas (ej. `<#noautoesc>`), siguiendo la guía de `CLAUDE.md` de no asumir que un fix funciona sin confirmación — varias hipótesis previas en este proyecto resultaron no ser la causa real. En su lugar, el siguiente paso es inspeccionar directamente el texto crudo que genera la plantilla (vía la acción de vista previa/descarga del documento electrónico generado, visible en `custbody_psg_ei_generated_edoc` tras un render exitoso) para ver el error real en vez de seguir teorizando sobre el motor de escapado.
- **Problemas y riesgos:** Sigue sin poder descartarse que la causa de "JSON inválido" sea algo distinto al escapado de caracteres (ej. algún otro campo de cabecera, o el propio campo `custbody_sads_fama_cpago_payload` con contenido inesperado en el momento exacto de esa prueba). No hay evidencia directa todavía, solo inferencia a partir de mensajes de error.
- **Pendientes:** Regenerar el documento electrónico con la plantilla revertida y compartir el texto crudo generado (vista previa/descarga) para diagnosticar con evidencia directa en vez de hipótesis.

### 2026-09-24 (continuación) — Bisección descarta el escapado; corrección real en FiscalRegime del receptor

- **Estado:** en progreso
- **Objetivo o problema:** Aislar la causa real del "documento electrónico no es un archivo JSON con formato correcto", descartada la hipótesis de escapado.
- **Cambios realizados:** Se probó reducir la plantilla completa a únicamente `${transaction.custbody_sads_fama_cpago_payload}` (sin ningún tratamiento de escape) directamente en el editor de NetSuite. El documento se generó correctamente y el contenido crudo mostrado coincide exactamente con el JSON esperado — descarta por completo la hipótesis de auto-escapado del motor. El problema está en la cabecera que construye la propia plantilla, no en el campo. Comparando contra la plantilla de MySuite para Complemento de Pago (funcional en esta cuenta, aportada por el usuario), se encontró una diferencia concreta: `FiscalRegime` del receptor se leía de `satCodes.customerIndustryType`, un campo que el hook nativo de Customer Payment (`customerPayment.js`, bundle 436209) no llena — solo lo llena el hook de Invoice. Se corrigió para leerlo directo de `customer.custentity_mx_sat_industry_type[0..2]` (mismo criterio que MySuite), con guarda de longitud para no reventar si el valor viene más corto de 3 caracteres. De paso, se blindaron con `?json_string` todos los strings de cabecera que antes se interpolaban sin ningún escape (`Rfc`, `FiscalRegime`, `TaxZipCode`, `ExpeditionPlace`, `PaymentForm`, `Currency`), eliminando esa clase de riesgo independientemente de si era o no la causa real.
- **Archivos o componentes:** `facturama_customerpayment_template.ftl`.
- **Resultado:** Sin confirmar todavía en sandbox — pendiente de que el usuario reintente la generación completa.
- **Decisiones técnicas:** Se prefirió confiar en la plantilla MySuite (evidencia de funcionamiento real en esta cuenta) por encima de la lectura previa del código fuente del hook nativo (que pudo corresponder a una versión distinta del bundle a la instalada aquí).
- **Pendientes:** Confirmar si la corrección de `FiscalRegime` resuelve la generación del E-Document. Si persiste el error, la siguiente bisección natural es la inversa (cabecera completa, `RelatedDocuments` reemplazado por `[]` fijo) para terminar de aislarlo.

### 2026-09-24/25 — Generación del E-Document resuelta por debugging manual directo en sandbox

- **Estado:** resuelto (generación); pendiente timbrado real contra Facturama
- **Objetivo o problema:** Cerrar el bloqueo de generación del E-Document para Customer Payment.
- **Cambios realizados:** El usuario resolvió el bloqueo mediante debugging manual directo en el editor de la plantilla en NetSuite (registro `customrecord_psg_ei_template`, id 122 — no es el editor estándar de Advanced PDF/HTML Templates, es un registro propio del bundle de Electronic Invoicing, sin panel de preview disponible). La causa raíz exacta no quedó plenamente identificada — el asistente no tiene forma de ejecutar FreeMarker fuera de NetSuite, así que el diagnóstico se hizo por bisección (reducir la plantilla progresivamente) en vez de por inspección directa del render. Se corrigieron, además, dos defectos de contenido detectados en la versión que sí generó: (1) `RelatedDocuments` quedaba doblemente anidado (`[[...]]`) porque la plantilla envolvía en `[ ]` un campo que ya es un arreglo completo; (2) faltaba `"Currency"` en `Complemento.Payments[0]` (campo obligatorio según el ejemplo oficial de Facturama). Se reincorporaron las validaciones Fail-Fast (`<#stop>`) para los campos de cabecera requeridos, ahora usando las fuentes de campo confirmadas por la prueba real: `transaction.custbody_mx_customer_rfc` (Rfc receptor), `customer.custentity_mx_sat_industry_type[0..2]` (Régimen Fiscal receptor, en vez de `satCodes.customerIndustryType`), `transaction.custbody_mx_txn_sat_payment_method[0..1]` (Forma de Pago, en vez de `satCodes.paymentMethod`), `transaction.payment` (Monto, en vez de `transaction.total`). Se agregó de vuelta el nodo `Issuer` (ausente en el ejemplo de Facturama pero presente en la plantilla MySuite funcional, y necesario en la práctica).
- **Archivos o componentes:** `facturama_customerpayment_template.ftl`.
- **Resultado:** El documento electrónico se genera correctamente con datos reales de sandbox (pago 2052106, facturas 2052103/2052104). No se ha probado el timbrado real contra la API de Facturama con esta plantilla — la generación del E-Document y la aceptación del PAC son pasos distintos.
- **Decisiones técnicas:** Se prefirió confiar en la plantilla MySuite (funcionando de forma comprobada en esta cuenta) como fuente de verdad para el mapeo de campos, por encima del código fuente del hook nativo leído previamente (que pudo corresponder a una versión del bundle distinta a la instalada aquí).
- **Problemas y riesgos:** La causa raíz exacta del error "no es un archivo JSON con formato correcto" no quedó documentada con certeza (probablemente un error de sintaxis puntual, según indica el usuario, no las hipótesis de escapado o de origen de campo que se investigaron). Sin acceso a un motor FreeMarker local, el asistente no puede verificar sintácticamente una plantilla antes de probarla en NetSuite — ver la propuesta de entorno local de FreeMarker, iniciada en esta misma fecha, para cerrar esta brecha en plantillas futuras.
- **Pendientes:** Probar el timbrado real contra Facturama con el payload ya generado; confirmar si `"Folio": "CP"` (visto en una versión intermedia) debía ser un valor fijo o dinámico — la plantilla actual lo deja dinámico (`transaction.tranid`) por defecto, pendiente de confirmación del usuario; extender el mismo enfoque (User Event + plantilla adaptador puro + validación Fail-Fast) a Notas de Crédito.

### 2026-09-25 — Harness local de FreeMarker para validar plantillas antes de NetSuite

- **Estado:** completado (herramienta); en uso para el siguiente frente (Notas de Crédito)
- **Objetivo o problema:** Cerrar la brecha señalada en la entrada anterior: el asistente no podía ejecutar FreeMarker fuera de NetSuite, por lo que el debugging de plantillas dependía de bisección manual contra el sandbox real. FreeMarker es una librería Java de código abierto — se puede ejecutar localmente.
- **Cambios realizados:** Se creó `tools/freemarker-harness/`, un proyecto Maven independiente (Java 17, no forma parte de `pac_facturama_integration` ni se despliega) con una clase `FreemarkerHarness` que carga una plantilla `.ftl`, la renderiza contra un modelo de datos JSON reconstruido a mano (variables de nivel superior `transaction`/`customer`/`custom`/`companyinformation`, con la convención `{"__date__": "..."}` para fechas reales), imprime el resultado, y valida que sea JSON sintácticamente correcto usando `org.json` (con mensaje de error y posición exacta si no lo es, a diferencia del mensaje genérico de NetSuite). Se probó reconstruyendo el modelo del pago real 2052106 y corriéndolo contra la plantilla de Complemento de Pago ya funcional — el resultado coincide con lo generado en sandbox (estructura, `Issuer`, `RelatedDocuments` plano, `Currency`), confirmando que el harness reproduce el comportamiento real razonablemente bien.
- **Archivos o componentes:** `tools/freemarker-harness/` (`pom.xml`, `FreemarkerHarness.java`, `models/customerpayment_2052106.json`, `README.md`).
- **Resultado:** Herramienta funcional y validada contra un caso real conocido. Dependencias (FreeMarker 2.3.34, org.json) descargadas de Maven Central; requiere Java 17+ y Maven, ambos ya disponibles en el equipo de trabajo.
- **Decisiones técnicas:** El harness valida sintaxis FreeMarker y validez JSON del resultado — no sustituye la prueba final en sandbox, ya que el motor real de NetSuite puede exponer tipos de dato o resoluciones de campo (ej. cómo resuelve exactamente un campo LIST/RECORD) que un modelo reconstruido a mano no capture con 100% de fidelidad.
- **Pendientes:** Usar este flujo (reconstruir modelo → diseñar plantilla → validar local → probar en sandbox) para la siguiente plantilla, Notas de Crédito.

### 2026-09-25 (continuación) — Primera plantilla de Nota de Crédito y dos defectos reales encontrados en el harness

- **Estado:** validado en harness local; sin probar en sandbox
- **Objetivo o problema:** Aplicar el flujo de 5 pasos (ver `tools/freemarker-harness/README.md`) para construir `facturama_creditmemo_template.ftl` (CfdiType "E", NameId 2), usando como insumo la documentación de Facturama para Nota de Crédito, el par `mysuite_invoice_template.ftl`/`mysuite_credit_memo_template.ftl` (funcionales hoy en esta cuenta vía la integración anterior) y 6 XML reales: la Nota de Crédito id 2045699 (tranid 2941), su cliente (id 2083), su subsidiaria (id 4), la factura original relacionada (id 2041212, UUID `493df8f6-c341-45d1-b43d-4d82b4c4fa3e`) y el XML certificado que MySuite generó para ese caso.
- **Cambios realizados:** Se reconstruyó `tools/freemarker-harness/models/creditmemo_2045699.json` cruzando cada campo que consume `mysuite_credit_memo_template.ftl` contra el valor que efectivamente aparece en el XML certificado (no solo contra el XML de origen), lo que permitió confirmar por evidencia — no por inferencia — que `satCodes.customerIndustryType` ("612") y `satCodes.paymentTerm` ("PUE") SÍ se resuelven correctamente para este tipo de transacción (a diferencia del caso de Customer Payment, documentado el 2026-09-24, donde ese mismo campo llegaba vacío). Se diseñó `facturama_creditmemo_template.ftl` tomando como base `facturama_edocs_template.ftl` (la plantilla de Factura ya probada en producción, misma familia de hook) en vez de partir de cero, ajustando: `NameId`/`CfdiType` fijos a Nota de Crédito, el nodo `Relations` como obligatorio con `<#stop>` (a diferencia de Factura, donde es opcional — un Comprobante de Egreso sin CFDI relacionado es fiscalmente inválido), y la regla SAT de `mysuite_credit_memo_template.ftl` que fuerza `PaymentMethod="PUE"` cuando el Tipo de Relación es "07" (pago de anticipo).
- **Archivos o componentes:** `Tests/Pruebas de E Docs NS CLI API/Plantillas/facturama_creditmemo_template.ftl` (nuevo); `tools/freemarker-harness/models/creditmemo_2045699.json` (nuevo).
- **Resultado:** El harness renderiza sin excepciones y valida como JSON correcto. Los valores de salida se contrastaron uno a uno contra el XML certificado real (Folio, ExpeditionPlace, FiscalRegime de emisor y receptor, Relations.Type, Relations.Cfdis[0].Uuid, Items[0] con su IVA prorrateado) y coinciden, incluyendo el cierre aritmético Subtotal 1137.85 + IVA 182.06 = Total 1319.91.
- **Decisiones técnicas:** (1) Se incluyó el nodo `Issuer`, ausente en el ejemplo de Facturama para Nota de Crédito, replicando la lección de Complemento de Pago (el ejemplo de Facturama también lo omitía ahí y resultó ser obligatorio) — no confirmado todavía para este tipo de documento. (2) Se omitió `CurrencyExchangeRate`: no aparece en `facturama_edocs_template.ftl` (Factura, ya probada) pese a ser un campo genérico de CFDI; se prefirió no añadir un campo no probado en ningún punto de este proyecto. (3) `CfdiUse` se dejó dinámico vía `satCodes.cfdiUsage`, igual que Factura, en vez de fijarlo — sin caso real que confirme el valor para Nota de Crédito.
- **Problemas y riesgos (harness):** Se encontraron y corrigieron dos defectos reales durante la validación, ambos relevantes para cualquier plantilla futura: (1) el `?number` de FreeMarker revienta ("Can't convert this string to number") si el valor de origen en el modelo JSON ya es un número — solo funciona si el modelo representa esos campos como *string*, que es como NetSuite expone en la práctica los campos de sublista/calculados (`amount`, `rate`, `quantity`, `taxAmount`, `taxBaseAmount`, `taxRate`, índices `.line`/`.index`); quedó documentado en `tools/freemarker-harness/README.md`. (2) Interpolar un número sin `?c` (ej. `${subTotal}`) usa el formato de número del locale activo (`en_US` = separador de miles), lo que produce JSON inválido en cualquier monto ≥ 1000 (`1,319.91` no es un número JSON válido) — se corrigió en la plantilla nueva. **Este mismo patrón (`${subTotal}`, `${Total_Item}` sin `?c`) existe hoy, sin corregir, en `facturama_edocs_template.ftl` (Factura, ya en producción)** — no ha fallado ahí porque NetSuite aparentemente no aplica agrupación por miles al interpolar, a diferencia del `Configuration` por defecto de este harness; sigue siendo una dependencia implícita, no verificada explícitamente.
- **Pendientes:** Confirmar en sandbox si `satCodes.*` del hook nativo de Nota de Crédito coincide con lo asumido aquí (idealmente repitiendo la técnica de `diagnostico_dump_contexto.ftl` contra la transacción real 2045699, antes de dar por buena la plantilla) — no se ha ejecutado esa validación todavía, el modelo se reconstruyó cruzando MySuite contra su propio XML certificado, no contra un volcado directo del hook de Facturama. Confirmar la inclusión de `Issuer` y el valor de `CfdiUse`. Evaluar si conviene blindar `${subTotal}`/`${Total_Item}` con `?c` también en `facturama_edocs_template.ftl`. Generar el E-Document real en sandbox recién después de lo anterior.

### 2026-09-25 (continuación) — `?is_hash`/`?keys` no son confiables contra el binding real de NetSuite; primer intento de timbrado de Nota de Crédito rechazado por el PAC

- **Estado:** en progreso — bloqueado en timbrado real, herramienta de diagnóstico corregida
- **Objetivo o problema:** El usuario ejecutó `diagnostico_dump_contexto.ftl` contra la transacción real 2045699 (Nota de Crédito) y obtuvo `"custom": "[hash sin claves enumerables o vacío]"`. Se construyó una sonda dirigida por nombre (`diagnostico_sonda_creditmemo.ftl`, sin depender de `?keys`) para distinguir "custom no tiene esos campos" de "custom no es enumerable pero sí responde a acceso directo" — el resultado fue el MISMO mensaje repetido para los ~30 campos probados, incluyendo una fecha (`transaction.trandate`) y strings simples (`customer.custentity_mx_rfc`), lo cual descarta que sean realmente hashes vacíos: ningún `?is_hash`/`?keys` puede ser correcto si clasifica una fecha real como "hash sin claves". Conclusión: el object-wrapper real de NetSuite para `transaction`/`customer`/`custom` no implementa la introspección de tipo (`?is_hash`, `?is_string`, `?keys`) de forma compatible con el `DefaultObjectWrapper` de FreeMarker contra el que se validó la v1 del harness — un defecto de diseño de la herramienta de diagnóstico, no un hallazgo sobre los datos reales. Ninguna plantilla que funciona hoy en este proyecto usa esos builtins de introspección — todas acceden por nombre directo, que es justo lo que la v1 no hacía.
- **Cambios realizados:** Se creó `tools/freemarker-harness/diagnostico_sonda_creditmemo_v2.ftl` — misma cobertura de campos que la v1, pero solo con interpolación directa (`${...!"..."}`) y `<#attempt>/<#recover>` por línea, sin ningún builtin de introspección. Validado en el harness local contra el modelo sintético existente. Pendiente de que el usuario la corra contra la transacción real — es la que debe reemplazar a la v1 y a la sonda v1 (ambas basadas en `?keys`, ahora sabidas no confiables contra NetSuite real).
- **Hallazgo independiente (timbrado real):** El usuario probó `facturama_creditmemo_template.ftl` en sandbox. La generación del E-Document fue exitosa (JSON limpio, coincide con lo validado en harness), pero el timbrado contra Facturama fue rechazado: `PAC - 400 Bad Request... {"cfdiToCreate":["Unexpected character encountered while parsing value: <. Path '', line 0, position 0."]}`. Esto indica que Facturama recibió contenido que empieza con `<` (típico de XML o una página HTML de error), no el JSON mostrado por el editor de NetSuite. Se revisó `pi_sads_fama_connector.js` completo: `send()` no tiene ninguna rama condicional por `CfdiType` — reenvía `plugInContext.eInvoiceContent` tal cual como cuerpo del POST, sin transformarlo — así que un bug del conector específico de Nota de Crédito queda descartado por lectura directa del código, no por suposición. La hipótesis con más sustento es que el contenido real que el framework de NetSuite entregó como `eInvoiceContent` en el momento del timbrado no correspondía a `facturama_creditmemo_template.ftl` (posible plantilla/config de E-Document todavía apuntando a otro origen para este tipo de transacción) — sin confirmar todavía, requiere revisión del usuario en la configuración de NetSuite.
- **Archivos o componentes:** `tools/freemarker-harness/diagnostico_sonda_creditmemo.ftl` (v1, defectuosa — se conserva como referencia del hallazgo, no usar); `tools/freemarker-harness/diagnostico_sonda_creditmemo_v2.ftl` (nuevo); `pi_sads_fama_connector.js`.
- **Decisiones técnicas:** Se agregó una guarda Fail-Fast en `pi_sads_fama_connector.js` inmediatamente después de `apiModule.safeParse(rawPayload)`: `safeParse` devuelve el string original sin tocar (no `null`, no excepción) cuando `JSON.parse` falla — sin guarda, ese string se reenviaba tal cual al PAC, que lo rechazaba con un 400 genérico sin pista de la causa. Ahora, si `originalPayload` sigue siendo string después de `safeParse`, se lanza una excepción clara (con los primeros 300 caracteres del contenido recibido) antes de intentar el POST, capturada por el `catch` existente y registrada vía `sads_fama_logger` — coherente con la política Fail-Fast del proyecto (no enviar al PAC un contenido que no se pudo validar como JSON).
- **Problemas y riesgos:** La causa raíz del contenido `<`-inicial en el timbrado real todavía no está confirmada — es una hipótesis de configuración (qué plantilla/origen está activo para Nota de Crédito), no un hecho verificado. La guarda Fail-Fast agregada no se ha desplegado ni probado en sandbox todavía.
- **Pendientes:** Desplegar y volver a intentar el timbrado para que, si el contenido sigue sin ser JSON válido, el log de `sads_fama_logger` muestre exactamente qué se recibió (en vez de descubrirlo por el rechazo del PAC). Confirmar en NetSuite qué E-Document Standard Template/origen está activo para Nota de Crédito. Correr `diagnostico_sonda_creditmemo_v2.ftl` contra la transacción real para cerrar la pregunta pendiente de `satCodes.*`.

### 2026-09-25 (continuación) — Primer timbrado real de Nota de Crédito exitoso; la causa del error 400 no era la plantilla

- **Estado:** resuelto (timbrado confirmado); pendiente aislar por qué falla el re-timbrado de una transacción con historial previo
- **Objetivo o problema:** Descartar de una vez si `facturama_creditmemo_template.ftl` tenía un defecto de contenido, comparándola línea por línea contra las tres plantillas Facturama que sí certifican hoy (`facturama_customerpayment_template.ftl`, `facturama_edocs_template.ftl`, y `facturama_cashsale_template.ftl` — esta última nunca se había guardado en el repo; el usuario la aportó en este punto y quedó anexada). El usuario aportó evidencia decisiva: acababa de timbrar con éxito CashSale, Invoice (con Relations) y Customer Payment con el mismo conector, lo que descarta un bug de `pi_sads_fama_connector.js` específico de Nota de Crédito.
- **Cambios realizados:** Se encontró una diferencia estructural real: a diferencia de `facturama_edocs_template.ftl` (que trata el nodo `Relations` como opcional, omitiéndolo si `custom.relatedCfdis` no tiene contenido), `facturama_creditmemo_template.ftl` lo exigía con `<#stop>` incondicional. Se reescribió para que el nodo `Relations` sea opcional/blindado exactamente igual que en Factura (variables `cfdiRelType`/`cfdisArray` inicializadas en `""` antes del `<#if>` para que referenciarlas después no reviente por variable indefinida cuando no hay relación). Validado en harness — sigue emitiendo `Relations` correctamente cuando el modelo lo trae.
- **Resultado real (más importante que el cambio anterior):** El usuario probó timbrar una Nota de Crédito NUEVA (transacción 2052113, relacionada a un Cash Sale creado en esta misma sesión de pruebas) contra Facturama sandbox — **certificación exitosa**: Folio `CUSTCRED2948`, UUID `27ebead4-79bc-42bc-893c-7e674ad97755`, XML/PDF certificados generados. El log de diagnóstico agregado en la entrada anterior (`'2. PAYLOAD A ENVIAR AL PAC'`) confirma que el payload, justo antes del POST, ya era JSON limpio.
- **Conclusión revisada:** El error 400 original ("Unexpected character... <") **no era un defecto de `facturama_creditmemo_template.ftl`** — la plantilla genera JSON válido y Facturama lo certifica sin problema contra una transacción nueva. El intento que falló fue un intento de RE-TIMBRAR la transacción 2045699, que ya tenía un XML certificado previo generado por MySuite antes de apuntar su configuración a Facturama. La hipótesis con más sustento ahora es que NetSuite, al re-generar el E-Document de una transacción con historial de E-Document previo, no siempre re-invoca la plantilla activa — puede servir contenido ya existente (el XML viejo de MySuite) en vez de regenerar. No confirmado todavía con el log real de ESE intento específico (el usuario pasó a probar con una transacción nueva antes de repetir 2045699 con el log ya desplegado).
- **Archivos o componentes:** `facturama_creditmemo_template.ftl` (nodo Relations ahora opcional); `facturama_cashsale_template.ftl` (nuevo, formalizado en el repo).
- **Problemas y riesgos:** Si la hipótesis de "re-timbrado de transacción con historial previo sirve contenido viejo" es correcta, cualquier reintento manual sobre una transacción que YA tuvo un E-Document (de MySuite o de un intento fallido de Facturama) podría fallar igual — un escenario real de operación (reintentar un timbrado que falló), no solo un caso de prueba. Pendiente de confirmar antes de considerar el flujo de Nota de Crédito completamente cerrado para producción.
- **Nota secundaria (no bloqueante):** La respuesta de Facturama para 2052113 muestra `"PaymentTerms":"23 - Novación"` como Forma de Pago — un valor SAT poco común para una venta ordinaria. El campo viene de `satCodes.paymentMethod`, el mismo usado sin cambios en las 4 plantillas que ya certifican en esta cuenta — no es un defecto de esta plantilla, pero vale la pena que el usuario confirme si "23 Novación" es el default correcto para este tipo de venta o un dato de configuración a revisar aparte.
- **Pendientes:** Repetir el intento de re-timbrado sobre 2045699 específicamente (ahora con el log de diagnóstico activo) para confirmar o descartar la hipótesis de contenido servido desde historial previo. Decidir si `Relations` en Nota de Crédito debe volver a ser Fail-Fast (obligatorio) ahora que se descartó que el `<#stop>` fuera la causa del error — pendiente de decisión del usuario. Confirmar el valor de Forma de Pago "23 Novación".

### 2026-09-25 (continuación) — Primera plantilla de Carta Porte (Comprobante de Traslado, sin cobro)

- **Estado:** validado en harness local contra un caso real; sin probar en sandbox
- **Objetivo o problema:** Construir `facturama_traslado_template.ftl` para Ejecución de Orden de Venta (`itemfulfillment`) — Comprobante de Traslado con Complemento Carta Porte 3.1, para traslado propio de mercancía (sin venta). Insumo: `Prompt_CartaPorte_sinCobro.md` (2412 líneas), aportado por el usuario ya siguiendo el flujo de 5 pasos — documentación de Facturama, la plantilla MySuite XML completa ya certificando en producción para este tipo de transacción, y el XML real de la transacción 1520482 (tranid 134059, 13 líneas de producto) junto con su salida certificada, cliente y subsidiaria relacionados.
- **Hallazgos por evidencia directa (no inferidos):** (1) En un Traslado propio el Receptor del CFDI es el propio emisor — la plantilla MySuite usa `custom.companyInfo.rfc`/`customCompanyInfo.custrecord_mx_sat_registered_name` para Receptor, nunca `customer.*`, y el XML certificado confirma `RFCReceptor` = RFC del emisor. (2) `CfdiType` debe ser `"T"` (Traslado), no `"I"` — la documentación de Facturama lo dice para este caso de uso y el XML certificado trae `TipoDeComprobante=TRASLADO`. (3) Los tres nodos más complejos del Complemento (Ubicaciones, Autotransporte, FiguraTransporte) ya llegan como JSON pre-armado en `custbody_drt_cp_json_ubicacion`/`_transporte`/`_figura_transporte` — no hace falta reconstruirlos, solo remapear de camelCase a las claves PascalCase de Facturama. (4) `transaction.item[N]`/`satCodes.items[N]` se indexan por el valor del campo `line` (0,5,10,15...), no por posición secuencial del arreglo — el modelo del harness tuvo que representarse como arreglo disperso (61 posiciones, 13 pobladas) para que la indexación coincidiera con el comportamiento real.
- **Decisión del usuario:** El arreglo `Items[]` de nivel CFDI usa la línea simbólica documentada por Facturama ("Transporte de carga por carretera", ProductCode fijo `78101800`, todo en cero) en vez de replicar el patrón de MySuite (listar las 13 líneas reales de producto en cero). Es una decisión explícita para la primera versión — permite validar el resto del Complemento sin apostar todavía a que Facturama acepte la variante de MySuite. Queda pendiente una segunda versión que sí mapee Items[] a los productos reales; si el PAC la rechaza, se vuelve a esta primera versión.
- **Alcance de esta primera versión:** Solo Autotransporte Federal (única modalidad con datos reales disponibles) — la plantilla lanza `<#stop>` si detecta Transporte Aéreo/Marítimo/Ferroviario. `RegistroISTMO`/`UbicacionPoloOrigen`/`UbicacionPoloDestino` y los atributos específicos de sector COFEPRIS quedan implementados de forma condicional/opcional pero sin ningún caso real que los ejercite. `Domicilio` de FiguraTransporte se omitió por no aparecer en el ejemplo documentado de Facturama, aunque la plantilla MySuite sí lo incluye.
- **Archivos o componentes:** `Tests/Pruebas de E Docs NS CLI API/Plantillas/facturama_traslado_template.ftl` (nuevo); `tools/freemarker-harness/models/traslado_1520482.json` (nuevo).
- **Resultado:** El harness renderiza sin excepciones y valida como JSON correcto. Cada valor de Ubicaciones (2), Mercancia (13, con BienesTransp/Descripcion/Cantidad/ClaveUnidad/PesoEnKg), Autotransporte y FiguraTransporte se contrastó uno a uno contra el XML certificado real y coincide exactamente.
- **Defecto de FreeMarker encontrado (ya corregido):** `(expr)!"" ?has_content` con espacio antes de `?has_content` NO encadena el builtin sobre `(expr)!""` — lo aplica sobre el literal `""` del lado derecho del operador `!`, y el `<#if>` termina evaluando el string original en vez de un booleano, reventando con "Expected a boolean, but this has evaluated to a string". Corregido envolviendo todo en un paréntesis adicional: `((expr)!"")?has_content`. Relevante para cualquier plantilla futura que use este patrón.
- **Decisiones técnicas no confirmadas:** `"NameId": "36"` — la documentación de Facturama solo lo muestra emparejado con `CfdiType: "I"` en su ejemplo completo; se combinó con `CfdiType: "T"` (confirmado por otra vía) porque NameId parece ser el selector de "plantilla Carta Porte" de Facturama independiente del tipo de comprobante, pero esta combinación específica no está probada. `"Currency": "XXX"` y `"CfdiUse": "S01"` replican la salida certificada real / el ejemplo de Facturama respectivamente, sin confirmación adicional del lado de Facturama todavía.
- **Pendientes:** Probar en sandbox contra Facturama. Confirmar el par NameId 36 + CfdiType T. Construir la segunda versión con Items[] mapeado a productos reales una vez aprobada esta. Extender a Transporte Aéreo/Marítimo/Ferroviario si el negocio lo requiere. Definir dónde vive la plantilla equivalente "Carta Porte como complemento de Factura" (la otra variante mencionada por el usuario, CfdiType I con cobro) — todavía no se ha empezado.

### 2026-09-25 (continuación) — Primera prueba real en sandbox: error genérico, dos campos obligatorios sin blindar y un error de arquitectura propio

- **Estado:** dos defectos corregidos y verificados en harness; falta reintentar en sandbox
- **Objetivo o problema:** El usuario generó una Ejecución de Orden de Venta nueva (transacción 2052117, tranid 189832) y la plantilla falló con un error genérico de NetSuite (`UNEXPECTED_ERROR`, sin mensaje, stack trace apuntando a `N/render.renderAsString` dentro del bundle 436209) — el mismo patrón opaco ya visto con Complemento de Pago.
- **Cambios realizados:** Se comparó el XML de la nueva transacción contra la primera (1520482) y se encontró que `custbody_drt_cp_pesobrutototal` no aparece en el dump (vacío), mientras que la plantilla lo usaba con `?number` sin ningún `?has_content` de por medio. Reproducido en el harness quitando ese campo del modelo existente — mismo tipo de excepción exacta. Corregido con `<#stop>` Fail-Fast (campo obligatorio del esquema SAT, no debe rellenarse con 0 en silencio); se aprovechó para blindar `custbody_drt_cp_clave_unidadpeso` igual, mismo patrón sin evidencia de fallo todavía pero igualmente obligatorio y desprotegido.
- **Segundo hallazgo, más importante (error de arquitectura, no de datos faltantes):** Al reconstruir el modelo de la segunda transacción se encontró que `custcol_drt_cp_pesoenkg` tampoco aparece en su XML — pero investigar esto llevó a notar que la plantilla leía este campo (y `custcol_drt_cp_materialpeligroso`) desde una propiedad inventada en `custom.items[]` (`customItem.pesoEnKg`/`.materialPeligroso`), que MySuite — la única fuente probada — NUNCA usa: MySuite lee `item.custcol_drt_cp_pesoenkg` y `item.custcol_drt_cp_materialpeligroso` directo del sublist nativo. El harness no atrapó este error antes porque el modelo de la primera transacción se construyó de forma autoconsistente (mismo autor para plantilla y modelo, con los mismos nombres inventados en ambos lados) — validar contra un modelo propio no prueba que ese modelo corresponda a lo que NetSuite realmente expone. Se corrigió la plantilla para leer ambos campos de `item.custcol_drt_cp_*` (el sublist nativo), igual que MySuite, y se corrigió `Descripcion` de la misma forma (`item.item`, no `item.description` — coincide mejor con el XML certificado real, que mostraba el texto sin la "o" final de la descripción larga). Se añadió `<#stop>` Fail-Fast por línea si `custcol_drt_cp_pesoenkg` falta, identificando el artículo y el número de línea en el mensaje.
- **Archivos o componentes:** `facturama_traslado_template.ftl`; `tools/freemarker-harness/models/traslado_1520482.json` (reestructurado); `tools/freemarker-harness/models/traslado_2052117.json` (nuevo, segunda transacción real).
- **Resultado:** Ambas guardas se verificaron por separado en el harness — cada una produce un mensaje claro y específico (qué campo falta, en qué línea) en vez del error genérico de NetSuite. La transacción 2052117 real tiene AMBOS campos vacíos (`custbody_drt_cp_pesobrutototal` a nivel cabecera y `custcol_drt_cp_pesoenkg` en las 11 líneas) — esto ahora es un problema de captura de datos en NetSuite, no un defecto de la plantilla.
- **Problemas y riesgos:** El patrón que causó el error de arquitectura (inventar una propiedad en `custom.items[]` en vez de verificar contra qué objeto la usa MySuite) es una clase de error que puede repetirse en cualquier campo nuevo de cualquier plantilla futura — un modelo de harness autoconsistente no lo detecta. Mitigación aplicada: para cada campo nuevo, confirmar explícitamente en la plantilla MySuite de referencia si se lee de `custom.items[]` o de `transaction.item[]`/`custcol_*`, no asumir.
- **Pendientes:** El usuario debe capturar `custbody_drt_cp_pesobrutototal` y `custcol_drt_cp_pesoenkg` (las 11 líneas) en la transacción 2052117 antes de poder reintentar la generación. Confirmar en sandbox que, con esos datos completos, la plantilla genera y timbra correctamente.

